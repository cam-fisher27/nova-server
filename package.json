{
  "name": "clinicos-server",
  "version": "2.0.0",
  "description": "ClinicOS API server — CloudRx proxy + Supabase gateway",
  "main": "clinicos-server.js",
  "scripts": {
    "start": "node clinicos-server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
